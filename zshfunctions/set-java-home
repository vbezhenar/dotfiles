if [[ -n $1 && ! -e ~/apps/java/jdk-$1 ]] {
  print >&2 "Usage: $0 1.8|11|17|21"
  return 1
}
if [[ -n $JAVA_HOME ]] {
  path=(${path:#$JAVA_HOME/bin})
}
if [[ -n $1 ]] {
  export JAVA_HOME=~/apps/java/jdk-$1
  path=($JAVA_HOME/bin $path)
} else {
  unset JAVA_HOME
}
